import React from "react";

type ComponentProps = {
  
};

const Component: React.FC<ComponentProps> = (props) => {
  return (
    <div>
      Component Template
    </div>
  );
};

export default Component;